# Ciencia y tecnología

::: callout-warning
### [🤔 ¿Sabías que ...?]{style="font-size: 1.8em; color: #c41e3a;"} 

- **En 2023, CLM invirtió más de 330 millones de euros en I+D. El sector empresarial recibió más de la mitad de estos fondos.**
- **En CLM hay casi tantos móviles como habitantes. ¡Más de 98 líneas por cada 100 personas!**
- **Prácticamente todas las empresas de CLM tienen internet, pero apenas el 8 % usan inteligencia artificial (IA).**
:::

```{r librerias}
library(readxl)
library(tidyverse)
library(knitr)
library(kableExtra)
#library(knitr)
library(formattable)
```


```{r colores}
# colores básicos institucionales
azul_jccm <- "#002855"
gris_jccm <- "#777779"
rojo_spain <- "#820046"
azul_odd <- "#64c9e1"
dorado <- "#b49600"

ab <- "#A8D5BA"
cr <- "#6FBF73"
cu <-	"#4F7942"
gu <-	"#2E8B57"
to <-	"#1E5631"

# matices (mismos colores al 15 %)
azul_junta_matiz<- "#c9d6e3"
rojo_spain_matiz <- "#e6b7b7"
gris_jccm_matiz <- "#d5d5d6"
azul_odd <- "#e8f7fb"

```

```{r}
# Leer el archivo Excel y la pestaña especificada
datos <- read_excel("data/datos01_ciencia_tecnologia.xlsx", sheet = "ID_estad_act_i+d_t1")
```

## La inversión en I+D

[**CLM destinó más de 330 millones de euros a investigación y desarrollo (I+D) en 2023**]{.blue}

Eso equivale al 1,5 % del total nacional, una inversión que impulsa la innovación desde empresas, universidades y 
administraciones públicas.

::::::: columns
::: {.column width="47%"}
```{r}
gastos <- as.numeric(datos[1, 2:6])
sectores <- c("Adm. Públicas", "Enseñanza superior", "Empresas", "IPSFL", "Total")

# Crear data frame
df_clm <- data.frame(
  Sector = sectores,
  Gasto = gastos
) |>
  mutate(Porcentaje = round(Gasto / Gasto[Sector == "Total"] * 100, 2))

# Separar "Total" y ordenar el resto
df_clm_ordenado <- df_clm |>
  filter(Sector != "Total") |>
  arrange(desc(Gasto)) |>
  bind_rows(df_clm |> filter(Sector == "Total"))  # añadir Total al final

# Mostrar tabla en LaTeX y HTML
df_clm_ordenado |>
  kable(
    format = ifelse(knitr::is_latex_output(), "latex", "html"),
    booktabs = TRUE,
    caption = "Gastos en I+D interna por sector en CLM (2023)") |>
  kable_styling(
    bootstrap_options = c("condensed", "responsive")) |>
  row_spec(0, background = rojo_spain, color = "white") |>
  row_spec(nrow(df_clm_ordenado), bold = TRUE)
```
:::

::: {.column width="6%"}
:::

:::: {.column width="47%"}
::: callout-note
### [El sector empresarial recibe más de la mitad de la inversión en I+D]{style="font-size: 1.2em; color: azul_jccm;"}

En 2023, las empresas absorbieron el 57,1 % de la inversión total en I+D, muy por delante de la enseñanza 
superior (27,4 %) y las administraciones públicas (15,3 %).
:::
::::
:::::::


[**El 42 % del personal en I+D en CLM son mujeres**]{.blue}

En CLM, la I+D no solo se hacen en laboratorios universitarios o centros públicos. En 2023, **casi 4.000 personas 
trabajaron en I+D a jornada completa (EJC) en la región** y el 45 % de ellas lo hicieron desde el sector empresarial, que lidera el 
esfuerzo humano en innovación.

Le siguen las **universidades**, con un 36,2 %, y las **administraciones públicas**, con un 18,1 %.

**Las mujeres representan el 42 % del personal dedicado a I+D en CLM**, una cifra que muestra el papel cada vez más 
relevante de las investigadoras en el ecosistema científico regional.

[**Las mujeres en CLM representan, aproximadamente, el 42 % del personal total en EJC **]{.blue}

::: callout-note
### [CLM innova por encima de la media]{style="font-size: 1.2em; color: azul_jccm;"}

Las empresas de CLM que invierten en innovación y en actividades de I+D lo hacen con más intensidad que la media española.
En 2022, la región **superó a España en dos de los tres indicadores clave de innovación**, medidos como el porcentaje que 
representan los gastos en innovación sobre la cifra de negocios
:::


```{r}
#| label: lollipop-innovacion
#| fig-cap: "Fuente: INE (2022)"
#| fig-cap-location: bottom

library(ggplot2)
library(dplyr)

# Datos
datos <- data.frame(
  Categoria = rep(c(
    "Total de empresas",
    "Empresas con gasto en innovación",
    "Empresas con actividades de I+D"
  ), each = 2),
  Región = rep(c("España", "CLM"), times = 3),
  Intensidad = c(0.93, 0.75, 1.61, 1.80, 1.90, 2.75)
)

# Dataset con segmentos
segmentos <- datos |>
  group_by(Categoria) |>
  summarise(
    x_min = min(Intensidad),
    x_max = max(Intensidad),
    .groups = "drop"
  )

# Gráfico comparativo
ggplot(datos, aes(y = Categoria, x = Intensidad, color = Región)) +
  # segmentos entre España y CLM
  geom_segment(
    data = segmentos,
    aes(x = x_min, xend = x_max, y = Categoria, yend = Categoria),
    inherit.aes = FALSE,
    color = gris_jccm,
    size = 1.2
  ) +
  # puntos
  geom_point(size = 4) +
  # etiquetas con ajuste automático para evitar solapamiento
  geom_text(
    aes(label = Intensidad),
    position = position_nudge(y = c(-0.12, 0.12)),  # desplaza arriba/abajo
    hjust = -0.3,
    size = 3.5
  ) +
  # Paleta de colores para las regiones
  scale_color_manual(values = c("CLM" = azul_jccm, "España" = rojo_spain)) +
  labs(
    title = "Intensidad de innovación CLM vs España (2022)",
    x = "Gastos innovación/Cifra de negocios (%)",
    y = NULL,
    color = "Región"   # ✅ título de la leyenda corregido
  ) +
  xlim(0, max(datos$Intensidad, na.rm = TRUE) + 0.5) +
  theme_minimal(base_size = 8) +
  theme(
    legend.position = "right",
    plot.title = element_text(size = 13, face = "plain", color = gris_jccm),
    axis.text.y = element_text(size = 10),
    axis.text.x = element_text(size = 9),
    aspect.ratio = 0.50,
    plot.margin = margin(t = 5, r = 5, b = 0, l = 5)  # ✅ elimina espacio inferior
  )
```

## El desigual mapa de la conectividad en CLM

[**Guadalajara y Toledo, líderes en telefonía fija y banda ancha**]{.blue}

Aunque CLM avanza en conectividad digital, no todas sus provincias están al mismo nivel.
Un análisis de los **servicios de telecomunicaciones** revela fuertes contrastes territoriales: 
**Guadalajara y Toledo** destacan por su buen desempeño en telefonía fija y banda ancha, 
mientras que otras zonas aún tienen margen de mejora.

:::::: columns
:::: {.column width="34%"}
::: callout-note
### [Con más de 98 líneas móviles por cada 100 habitantes, CLM avanza en conectividad]{style="font-size: 1.2em; color: azul_jccm;"}

Sin embargo, a pesar de este buen dato, la comunidad todavía se sitúa por debajo de la media nacional en infraestructura digital, lo que señala que aún hay margen para seguir mejorando en el acceso y calidad de las telecomunicaciones.
:::
::::

::: {.column width="66%"}
```{r}
#| label: teleco-clm
#| fig-cap: "Fuente INE, CNMC (2023)"
# fig-width: 8.5        # <- un poco más ancho
# fig-height: 7.5       # <- un poco más alto
# out-width: "100%"     # <- ocupa todo el ancho del contenedor

# Datos
library(fmsb)
teleco_servicios <- read_excel("data/datos01_ciencia_tecnologia.xlsx", 
                               sheet= "TELECO_servicios")

nombres_provincias <- teleco_servicios[[1]] # Guarda la primera columna (nombres)
datos_radar_crudo <- as.data.frame(teleco_servicios[, c(2:4)]) # Crea un DF con el resto de columnas
rownames(datos_radar_crudo) <- nombres_provincias # Asigna los nombres de las filas

# 4. Preparar los datos para la función radarchart()
# La primera fila debe ser el máximo y la segunda el mínimo de cada variable.
# La escala de 0 a 110 funciona bien para estos datos.
datos_preparados <- rbind(rep(110, 4), rep(0, 4), datos_radar_crudo)

# 5. Definir los colores para cada una de las 7 líneas
colores_lineas <- c(ab, cr, cu, gu, to, azul_jccm, rojo_spain)


# 6.Ajustes de texto y márgenes
op <- par(
  font.main = 1,          # título sin negrita
  cex.main  = 1,       # tamaño del título
  cex.axis  = 1,       # tamaño de las etiquetas de la escala (0,20,40,...)
#  mar       = c(6, 4, 4, 2) + 0.1  # más margen inferior para la leyenda
  mar       = c(2, 2, 2, 2)  # más margen inferior para la leyenda
)

# 7.Dibujar el gráfico de radar
radarchart(datos_preparados,
axistype = 1,
pcol = colores_lineas,
plty = 1,
cglcol = "grey70",
cglty = 1,
axislabcol = "grey",
caxislabels = seq(0, 110, 20),
title = NULL,
vlcex = 1.5)


# 8.Título (gris y sin negrita)
title(main = "Líneas por cada 100 habitantes (2023)",
      col.main = gris_jccm)


# 9.Añadir la leyenda para identificar las líneas
legend("topright", legend = rownames(datos_preparados[3:9,]),
bty = "n", pch = 20, col = colores_lineas,
text.col = "black", cex = 1, pt.cex =3 ,x.intersp = 0.9,y.intersp = 1.0)
```
:::
::::::

[**Toledo lidera el despliegue de redes de nueva generación (Next Generation Access, NGA) en CLM**]{.blue}

CLM sigue avanzando en infraestructura digital: ya representa **el 4,6 % del total nacional** en redes de 
nueva generación.

La **región cuenta con 3,9 millones de accesos NGA** instalados, lo que la sitúa 
en una posición relevante dentro del despliegue tecnológico en España.

Pero no todas las provincias están igual de conectadas. Toledo es líder con casi 1,3 millones de 
accesos, mientras que Cuenca apenas tiene medio millón.




## Empresas conectadas, pero poco digitalizadas

[**La paradoja digital de las empresas en CLM: alta conectividad, baja IA**]{.blue}

En CLM, la conectividad básica está prácticamente garantizada: **el 98,8 % de las empresas tiene acceso a Internet y el 99,7 % dispone de ordenadores**.

Sin embargo, solo la mitad del personal los utiliza en su trabajo diario, lo que revela un uso 
limitado de las herramientas digitales.

Ocho de cada diez empresas tienen página web, pero la adopción de tecnologías más 
avanzadas sigue siendo baja:

+ Solo el 21,2 % realiza analítica de datos internamente.
+ Apenas el 7,6 % utiliza IA.


```{r}
tic_mas10_tablas <- read_excel("data/datos01_ciencia_tecnologia.xlsx", 
                               sheet= "TIC_mas10_tablas")
```

:::::: columns
::: {.column width="66%"}
```{r}
# filtar las filas de la 6 a la 12 del objeto tic_mas10_tablas
tic_transf_digital <- tic_mas10_tablas[6:11, ]

tic_long <- tic_transf_digital |>
  pivot_longer(cols = c(`CLM (%)`, `España (%)`),
               names_to = "Region",
               values_to = "Valor")

# Gráfico
ggplot(tic_long, aes(x = Indicador, y = Valor, fill = Region)) +
  geom_col(position = "dodge", width = 0.7) +
  geom_text(aes(label = paste0(Valor, "%")),
            position = position_dodge(width = 0.7),
            hjust = -0.2, size = 3.2) +   
  scale_fill_manual(values = c("CLM (%)" = azul_jccm, "España (%)" = rojo_spain)) +
  labs(
    title = " ",
    x = NULL,
    y = "Porcentaje (%)",
    fill = "Región"
  ) +
  coord_flip() +
  theme_minimal(base_size = 10) +
  theme(
    legend.position = "bottom",
    legend.title = element_text(size = 10),
    legend.text  = element_text(size = 9),
    plot.title   = element_text(size = 12, face = "bold", hjust = 0.5),
    axis.text.y  = element_text(size = 9),
    axis.title.x = element_text(size = 10)
  )
```
:::

:::: {.column width="34%"}
::: callout-note
### [El teletrabajo no despega en CLM]{style="font-size: 1.2em; color: azul_jccm;"}

Aunque la tecnología está cada vez más presente en las empresas, solo el 5,95 
% de las personas trabajadoras en CLM teletrabaja de forma regular.
:::
::::
::::::

::: callout-note
### [Mujeres en tecnología, por encima de la media]{style="font-size: 1.2em; color: azul_jccm;"}

En las empresas de CLM que cuentan con personal especializado en TIC, el 45,9 % son mujeres, superando el promedio estatal del 41,9 %.
:::

```{r}
#| label: tabla-tic-especialistas
#| tbl-cap: "Indicadores sobre especialistas en TIC en empresas (%)"

library(tibble)

tic_especialistas <- tic_mas10_tablas[12:14, ]

knitr::kable(tic_especialistas, format = ifelse(knitr::is_latex_output(), "latex", "html"),
             booktabs = TRUE,
             caption = "Indicadores sobre especialistas en TIC en empresas (%)",
             col.names = c("Indicador", "CLM (%)", "España (%)")) |>
  kable_styling(
    latex_options = c("striped", "hold_position"),
    bootstrap_options = c("striped", "hover", "condensed", "responsive"),
    full_width = FALSE,
    position = "center"
  ) |>
  row_spec(0, background = rojo_spain, color = "white")|>
  footnote(general= "(i) Porcentaje sobre el total de empresas; (ii) Porcentaje sobre el total de empresas con conexión a Internet;     (iii) Porcentaje sobre el total de empresas que emplean especialistas en TIC")


```


